# Cross-version 393 ↔ 765 validation (both directions, real client + real server)

This is the TRANSLATED track. Every packet is decoded, translated and re-encoded;
nothing is byte-forwarded. Separate from the native 393 ↔ 393 DIRECT result in
`RESULTS-393-NATIVE.md`, which proves nothing about translation.

Conduit commit under test: `69535b3`. Build: `javac --release 21` over
`src/main`, resources copied into `out-run`, started fresh with
`-Dconduit.trace=true`.

## Direction A — 1.13 client → Conduit → 1.20.4 server

| Piece | Detail |
|---|---|
| Client | Official Minecraft **1.13** (protocol 393), offline, `ConduitTest` |
| Proxy | Conduit `127.0.0.1:25561`, `config/conduit-393-to-765.toml` |
| Backend | Official Minecraft **1.20.4** server (protocol 765), port 25604 |
| Result | **TRANSLATED — gameplay verified, no unexplained disconnect** |

## Direction B — 1.20.4 client → Conduit → 1.13 server

| Piece | Detail |
|---|---|
| Client | Official Minecraft **1.20.4** (protocol 765), offline, `ConduitXV` |
| Proxy | Conduit `127.0.0.1:25562`, `config/conduit-765-to-393.toml` |
| Backend | Official Minecraft **1.13** server (protocol 393), port 25613 |
| Result | **TRANSLATED — gameplay verified, no unexplained disconnect** |

Both directions ran simultaneously, through a scripted gameplay burst on each
backend (creative mode, an enchanted and damaged and custom-named sword, a stack
of stone, helmet and off-hand equipment, a named baby zombie, a creeper, a
potion effect, a 49-block `fill` of oak stairs with `facing=east,half=top`, a
chest, and a `/say`), with **zero translation failures in either direction**.

## What the item layer is

An item is carried as an identifier, a count and its NBT compound body, and
crosses the pair by **name**. Both the name tables and the id tables are
generated by `tools/gen_mappings.py` from the reports the official server jars
emit about themselves (`--reports`); nothing is copied from another proxy and
nothing is inferred from id arithmetic. All 785 of 1.13's items map. An item
with no counterpart on the target — netherite toward 1.13 — becomes an empty
slot rather than whatever item holds that number over there.

Block states are generated the same way, matched by block **name and property
set**, so facing / half / shape / open / powered / waterlogged survive the
crossing instead of collapsing to a default state. All **8582** of 1.13's block
states round-trip 393 → 765 → 393 exactly. Property domains that genuinely
changed are handled explicitly: wall sides went boolean → none/low/tall in 1.16,
and a filled cauldron became its own block in 1.19.

## Wire-format bugs a real client found, that unit tests did not

The item slot has **four** forms across this range, and both boundaries were
initially wrong in the same way on both sides of the codec, so a self-consistent
unit test passed while the wire was wrong:

```
393        itemId:i16 (-1 empty)  count:i8  nbt(NAMED root)
404..763   present:bool  itemId:VarInt  count:i8  nbt(NAMED root)
764..765   same fields, nameless NBT root (1.20.2 dropped the root name)
766+       count:VarInt  itemId:VarInt  data components
```

- The `present` boolean arrived in **1.13.2**, not 1.13. Reading a real 1.13
  server's dropped-item metadata with the 1.13.2 layout ate one byte too many
  and tripped over the metadata terminator.
- The count-first layout arrived in **1.20.5** with data components, **not** in
  1.20.4. Writing it to a 1.20.4 client made it read the count as the `present`
  flag and then hit the next field as an NBT tag type. The client reports that
  as nothing but `DecoderException: Loading NBT data` and drops the connection
  two seconds into the world.

The 1.13 **Spawn Object** type field is its own ancient enumeration, not the
entity registry: a boat is 1, a dropped item is 2, an arrow is 60. The generated
table had been built from registry indices, so all but two object types were
unmapped and every dropped item, arrow, TNT and minecart spawn was silently
dropped.

Text components are NBT from 1.20.3 on, and two encoding details bite:
- A translation's arguments can hold a bare string next to a nested component
  (`commands.give.success.single`). NBT lists are homogeneous, so modern
  Minecraft promotes every element to a compound under an empty key. Writing the
  list with the first element's type killed a 1.20.4 client on every `/give`.
- A list of numbers is stored as a `TAG_Int_Array`, not a list
  (`commands.fill.success`). Discarding it produced JSON whose `with` was a
  string where the client requires an array, and a **1.13** client closes the
  connection on malformed component JSON.

Both regressions are now pinned by tests built from the captured bytes.

## Why the session used to end

Conduit is fail-closed: an unrecognised packet id ends the session rather than
being forwarded blind. That is the right default, but it means every id a
backend can emit needs a decision. Three ordinary events were ending sessions:

- **Set Passengers** (1.13 `0x46`) — a real 1.13 server sends it within a minute.
- **Disguised Chat** (1.20.4 `0x1C`) — this is what `/say` and `/me` produce.
- **Player Abilities, serverbound** (1.13 `0x17`) — how a creative player toggles
  flight. A 1.13 player dropped out of the world the moment they pressed space
  twice.

The whole 1.13 clientbound play table and the 1.20.4-only clientbound additions
are now mapped, each with an explicit translate-or-drop decision.

## Inventory, containers and equipment — how they were exercised

The clientbound half was watched with the real clients: container contents, slot
updates, equipment and held-item changes all translate live.

The serverbound half needs someone to click, and synthetic OS input does not
reach a detached Minecraft window in this environment. `ItemGameplayProbe` and
`ItemGameplayProbe393` fill that gap: they are scripted clients, but Conduit and
the server behind each one are real, so their packets really are translated and
executed. Both backends confirm the results themselves:

```
# 1.20.4-protocol client → Conduit → 1.13 server
execute if block -89 4 -57 minecraft:stone run say ...
[Server] STONE_PLACED_BY_765_CLIENT_ON_113_SERVER

# 1.13-protocol client → Conduit → 1.20.4 server
execute if block -45 77 -18 minecraft:stone run say ...
[Server] STONE_PLACED_BY_393_CLIENT_ON_1204_SERVER
```

Container path, 765 → 393: a chest was placed and right-clicked, and the 1.13
server's Open Window arrived as `OPENED window=1 menu=2` — `generic_9x3`, the
correct 1.20.4 menu for a 27-slot chest — followed by its contents, a slot
click and a close.

Container path, 393 → 765: with the probe standing south of a freshly placed
chest (yaw 0 / face north), the 1.20.4 server's Open Screen arrives as
`OPENED window=1 type=minecraft:chest slots=27`. Conduit also synthesises the
**Confirm Transaction** a 1.13 client needs after inventory clicks, because
1.20.4 has no such packet. Container contents, slot updates, clicks and closes
all translate.

## Entity metadata and attributes

Metadata is translated by remapping **both** type ids and field indices. Neither
can be copied: 1.19 inserted VarLong at type 2, so a 765 Float would be read as
a 393 String and desynchronise the stream from that byte on. Indices shift by
entity class, and between these two releases exactly two insertions happened —
`Entity` gained pose and tickFrozen, `LivingEntity` gained beeStingers and
sleepingBedLocation — which yields one rule rather than a per-mob table:

```
0..5              Entity base          unchanged
non-living, 6+    entity-specific      +2
living, 6..10     LivingEntity block   +2
living, 11+       Mob / concrete class +4
```

Spawn packets are tracked per session so the translator knows which entities are
living. Attributes are mapped across the 1.16 rename (`generic.movementSpeed` ↔
`minecraft:generic.movement_speed`) with the count changing width, and entries
with no counterpart are dropped individually rather than failing the packet.

In the final run: 637 metadata and 382 attribute packets translated toward the
1.20.4 client, 219 and 54 toward the 1.13 client, no failures.

## Sounds

Translated, in both directions, through generated registry tables.

A sound travels as a registry index, and the registries disagree: 1.13 has 662
sound events, 1.20.4 has 1539, and the additions are spread through the list
rather than appended, so the same index is a different sound on each side. That
is why sounds were dropped here at first — dropping beat playing the wrong one.

`tools/gen_sounds.py` resolves every sound by name. 1.20.4's registry comes from
the server's own `--reports` dump. 1.13's cannot: its reports predate the
registry dump, so `tools/DumpRegistry.java` reads the registry out of the 1.13
jar instead, which is the same provenance the block and item tables already have.
Ids come from the registry's own by-id lookup, not from the order anything is
declared or iterated in.

**The dumper is calibrated, not trusted.** It reads ids out of an obfuscated jar,
so it is pointed at the one 1.13 registry whose ids are already known — the item
registry, which `items.json` does report — and its output must reproduce that
report exactly, all 785 entries in order. Only then is the sound dump used.

**This table was wrong once, and the way it was wrong is worth recording.** It
was first built from the declaration order of the jar's sound-event holder and
checked against 1.14's registry, on the assumption that a registry's order never
changes once fixed. It does change: 1.13's sound registry is alphabetical by
identifier and 1.14's is not, so that check passed a table in which **448 of 662
ids were wrong** — two sounds in three would have played as something else. The
cross-version check has been removed, because it cannot tell a right table from a
wrong one; the calibration above can, and `SoundTranslationTests` pins id 15,
which is one of the ids that was wrong.

| | |
|---|---|
| 1.13 sound events | 662 |
| 1.20.4 sound events | 1539 |
| mapped both ways | 658 |
| renamed by Mojang | 5 (`zombie_pigman.*` → `zombified_piglin.*`, `music.nether` → `music.nether.nether_wastes`) |
| removed after 1.13 | 4 parrot imitations (`enderman`, `polar_bear`, `wolf`, `zombie_pigman`) |

Named Sound Effect is translated too. 1.19.3 removed that packet and folded it
into Sound Effect's inline form, so toward 1.20.4 a 1.13 Named Sound Effect
becomes an inline Sound Effect, and toward 1.13 a sound whose name 1.13's
registry does not have — a plugin's own sound, or anything added since 1.13 —
is sent as Named Sound Effect rather than dropped. Only a sound that is neither
in the target registry nor expressible as a name is lost.

Entity Sound Effect stays unsupported: 1.13 has no packet that plays a sound on
an entity.

Not verified by a human listening. The tables and the packet translation are
covered by `SoundTranslationTests`; no real-client audio check was run.

## Particles

Translated, in both directions, through generated registry tables.

Particles failed on three counts at once, which is why they outlasted sounds:
the registry index means a different particle on each side (1.13 has 50 types,
1.20.4 has 101), the packet's own layout changed, and four particles carry a
payload naming the sender's own registries.

```text
1.13  (393)  i32 id | bool | f32 x | f32 y | f32 z | f32 off*3 | f32 data  | i32 count | payload
1.20.4 (765) VarInt | bool | f64 x | f64 y | f64 z | f32 off*3 | f32 speed | i32 count | payload
```

So the id widens to a VarInt and the position to doubles: forwarding the bytes
could never have worked even where an id happened to agree.

| Payload | Carried by | Handling |
|---|---|---|
| block state | `block`, `falling_dust` | translated through the same block table the chunk path uses |
| item stack | `item` | translated through `ItemCodec` |
| four floats | `dust` | red, green, blue and scale mean the same on both sides |
| none | the other 46 | nothing follows the count |

A 1.20.4 particle whose payload 1.13 has no shape for — `dust_color_transition`,
`vibration`, `sculk_charge` — has no 1.13 id either, so the id lookup drops it
before its payload is ever read.

| | |
|---|---|
| 1.13 particle types | 50 |
| 1.20.4 particle types | 101 |
| mapped both ways | 49 |
| no counterpart | `barrier`, which 1.18 replaced with `block_marker` — a different particle taking a block state, not a rename this table could make |

Same provenance and the same calibration as sounds: `tools/gen_particles.py` from
a `tools/DumpRegistry.java` dump of the 1.13 jar, with the dumper required to
reproduce `items.json` exactly first. The particle table was generated with the
by-id lookup from the start and so never had the fault the sound table had.

Not verified by a human watching. Covered by `ParticleTranslationTests`.

## Deliberately unsupported

Each of these is recognised and dropped on purpose, so the drop is a decision
and not a fatal unknown:

- **Scoreboards, teams, titles, boss bars, the tab list, statistics, maps and
  trade lists** — display only.
- **Block-entity data and block actions** — these name block-entity and block
  registry entries that this pair has no verified mapping for. Cost: sign text
  and the chest-opening animation.
- **Recipes and advancements** — each era's own data pack.
- **Bundle delimiters, damage events, hurt animations, block-change
  acknowledgements** — no pre-1.19 equivalent; entity status still drives the
  hurt animation.

## What is still NOT proven

- **Clicking a container by hand** was never done: synthetic OS input does not
  reach the Minecraft window here, so the serverbound container path is verified
  through scripted clients against real servers, not through a human at a mouse.
- **Some entity types** still fail closed: name-based maps cover the common
  living/object set (including iron golem, zombified piglin, fireworks, command
  minecart, trader llama→llama, trident, glow item frame→item frame, chest boat
  →boat), but uncommon modern-only types remain unmapped.
- Not swept: villager trading, redstone behaviour over time, dimension changes,
  server switching under translation, resource packs, furnace and enchanting and
  anvil screens, the recipe book, hoppers, or any modded traffic.
- Only this one pair. 393 ↔ 765 working says nothing about any other pair.

## Core gameplay status (this pair)

**CORE GAMEPLAY: bidirectionally verified** with scripted probes against real
vanilla 1.13 / 1.20.4 servers (login, configuration bridge, world entry, chunks,
movement, block place/break, chest open/click/close, inventory, equipment,
metadata, attributes, health, chat, keepalive). Not FULL Minecraft compatibility:
scoreboards, titles, boss bars, block-entity NBT, recipes and advancements
remain intentionally unsupported. Sounds and particles are supported: see below.

## Reproducing

```
python tools/gen_mappings.py <1.13 reports> <1.20.4 reports>   # regenerate tables
java -jar work/mc1204/server.jar nogui     # JDK 17, port 25604
java -jar work/mc113/server.jar nogui      # JDK 8,  port 25613
java -Dconduit.trace=true -cp out-run gg.tame.conduit.launcher.Main config/conduit-393-to-765.toml
java -Dconduit.trace=true -cp out-run gg.tame.conduit.launcher.Main config/conduit-765-to-393.toml
powershell -File work/real-client-validation/launch-xv-393-765.ps1   # 1.13 client
powershell -File work/real-client-validation/launch-1.20.4.ps1       # 1.20.4 client
java -cp out-run gg.tame.conduit.tests.ItemGameplayProbe    127.0.0.1 25562 Prober
java -cp out-run gg.tame.conduit.tests.ItemGameplayProbe393 127.0.0.1 25561 Prober13
```

`-Dconduit.trace.bodies=true` additionally dumps the bytes Conduit emits. A
client that rejects a packet reports only its own decoder's complaint and
nothing about what it was fed, so that switch is what made the slot-format and
component bugs findable.

`-Dconduit.translate.discovery=true` is a diagnostic-only mode that keeps a
session alive past unhandled packets so one run enumerates every packet a real
server sends. Production stays fail-closed.
