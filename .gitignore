.gradle/
build/
out/
out-*/
run/
*.class
*.secret
sources.txt
test-sources.txt
main-sources.txt
lib/*.jar
lib/via/
conduit-start.log
conduit-start.err.log


# Runtime output: proxy/client logs and diagnostic dumps are never committed
logs/
dumps/
tools/__pycache__/
config/via/

# Via's own data folder, written beside whatever config started the proxy. Translation is on
# by default, so a run from the repo root creates this one.
/via/

# scripts/build-dist.ps1 output: a local test build, not the distZip distribution.
/dist/
