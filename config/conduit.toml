[listener]
host = "127.0.0.1"
port = 25565
max-frame-bytes = 1048576

[forwarding]
mode = "none"
# secret-file = "secrets/forwarding.secret"

[servers.lobby]
host = "127.0.0.1"
port = 25566

[routing]
initial = ["lobby"]
fallback = ["lobby"]
